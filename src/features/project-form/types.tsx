export interface FormValues {
  projectTitle: string;
  client: string;
  description: string;
  budget: number;
  startDate: string;
  dueDate: string;
  initialStatus: string;
  priorityLevel: string;
  progress: number;
  createdAt: string;
}